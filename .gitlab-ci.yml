stages:
  - test

cypress_tests:
  stage: test
  image: cypress/included:13.6.0
  script:
    - npm ci
    - npm run dev &
    - npx wait-on http://localhost:3000
    - npx cypress run
  when: manual
  allow_failure: false